<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light">
        <!-- Customize your theme here. -->

    </style>

    <style name="ProgressBar_Mini" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:maxHeight">50dip</item>
        <item name="android:minHeight">8dip</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:progressDrawable">@drawable/progressbar_mini</item>
    </style>

    <style name="logout_button" parent="@style/fb_button">
        <item name="android:background">@drawable/login_centre_register</item>
    </style>
    <style name="bind_fb" parent="@style/bindfb_button">
        <item name="android:background">@color/white</item>
    </style>
    <style name="bindfb_button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:minHeight">50dp</item>
    </style>

    <style name="fb_button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:minHeight">50dp</item>
    </style>

    <style name="StartTheme" parent="android:Theme.NoTitleBar.Fullscreen">
        <!--
        <item name="android:windowBackground">@drawable/balala_01</item>//@null黑屏  @drawable/icon放一张启动图片
         -->
        <item name="android:windowBackground">@drawable/balala_01</item>
        <item name="android:windowNoTitle">true</item>
        <!--<item name="android:windowIsTranslucent">true</item>//启动界面是否显示应用名称 true不显示 false显示-->
    </style>

    <!--自定义AlertDialog的样式-->
    <style name="Dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>
    <style name="DialogText">
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">15sp</item>
    </style>
    <style name="DialogText.Title">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#ffffff</item>
    </style>
</resources>
